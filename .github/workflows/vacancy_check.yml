name: UR Vacancy Checker

on:
  schedule:
    # 1時間ごと（毎時00分）に実行
    - cron: '0 * * * *'
  workflow_dispatch:
    # 手動実行を可能にする

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Tokyo # 日本時間 JST に設定
    steps:
      - name: 📦 リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 🐍 Pythonのセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 🌐 Chromeブラウザのインストール (最終安定版)
        run: |
          # Google Chrome (またはChromium) の安定版をインストール
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          
      - name: 📦 依存関係のインストール (WebDriver Managerを含む)
        run: |
          # requirements.txtの内容（requests, bs4, selenium, webdriver-manager）をインストール
          pip install -r requirements.txt

      - name: 🛠️ 空き情報チェックと通知の実行
        env:
          # SMTP情報を環境変数としてスクリプトに渡す
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
        run: |
          # 正しいファイル名で実行 (末尾の 's' なし)
          python check_vacancy.py
